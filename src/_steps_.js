/**
 * 1. create firebase project
 * 2. create web app
 * 3.npm install firebase
 * 4. create firebase_config.js file
 * 5.Build > Authentication > Get Started > Set up sign-in method 
 * 6. crate sign up and login route 
 */

/**
 * 1. Create a context  provider  file
 * 2. crate a context and set provider 
 * 3. set the children props 
 * 4. set context value
 * 5. set the provider
 */

/**
 *  Hosting 
 * ---------------
 * one time  per pc 
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * for each project one time 
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow) use space bar to select
 * 4. use an existing project
 * 5. select project very carefully 
 * 6. which project as public directory? dist // vite use korle hobe dist. crate react app use korle build
 * 7. configure as a single page app? yes
 * 8. configure deployment? no
 * 
 * For every time deploy 
 * 1. npm run build
 * 2. firebase deploy
 * 
 */